@model IEnumerable<WebApplication6.Models.Machines>

@{
    ViewData["Title"] = "Index";
}

<table class="table">
    <thead>
        <tr>
            <th>
                @Html.DisplayNameFor(model => model.MachineNo)
            </th>
            <th>
                Downtime
            </th>
        </tr>
    </thead>
    <tbody>
@foreach (var item in Model) {
        <tr>
            <td>
                @Html.DisplayFor(modelItem => item.MachineNo)
            </td>
            <td>
               
            </td>
        </tr>
}
    </tbody>
</table>
